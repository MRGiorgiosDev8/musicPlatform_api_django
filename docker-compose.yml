services:
  web:
    build: .
    ports:
      - "8000:8000"
    deploy:
      resources:
        limits:
          memory: 512M
    volumes:
      - ./static:/app/static
      - ./staticfiles:/app/staticfiles
    restart: unless-stopped
    env_file:
      - .env