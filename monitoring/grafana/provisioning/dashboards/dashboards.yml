apiVersion: 1

providers:
  - name: "RubySound Dashboards"
    orgId: 1
    folder: "RubySound"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 15
    options:
      path: /var/lib/grafana/dashboards
