![GitHub last commit](https://img.shields.io/github/last-commit/MRGiorgiosDev8/musicPlatform_api_django?color=%23e0115f)
![Repository size](https://img.shields.io/github/repo-size/MRGiorgiosDev8/musicPlatform_api_django?color=%23e0115f)
![Platform](https://img.shields.io/badge/platform-linux%20%7C%20macos%20%7C%20windows-%23e0115f)
![License](https://img.shields.io/github/license/MRGiorgiosDev8/musicPlatform_api_django?color=%23e0115f)
![Tests](https://img.shields.io/github/actions/workflow/status/MRGiorgiosDev8/musicPlatform_api_django/tests.yml?label=tests&logo=github&color=%23e0115f)
![Deploy](https://img.shields.io/github/actions/workflow/status/MRGiorgiosDev8/musicPlatform_api_django/main.yml?label=deploy&logo=github&color=%23e0115f)

---

# 🎸 RubySound.fm (Kubernetes Version)

Данная ветка проекта переведена на работу в среде Kubernetes. 

---

## 🛠 1. Подготовка (Установка инструментов)

Перед запуском убедитесь, что у вас установлены следующие инструменты:

- **Docker Desktop** — Скачать.
  - Linux (Ubuntu/Debian):
    ```bash
    sudo apt update
    sudo apt install docker.io
    sudo systemctl enable docker
    sudo systemctl start docker
    ```
- **Minikube** — Локальный Kubernetes-кластер для разработки.
  - Mac: `brew install minikube`
  - Windows: `choco install minikube`
  - Linux:
    ```bash
    curl -LO https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
    sudo install minikube-linux-amd64 /usr/local/bin/minikube
    ```
  - После установки запустите кластер:
    ```bash
    minikube start
    ```
  - Проверить статус:
    ```bash
    minikube status
    ```
- **kubectl** — Утилита для управления кластером.
  - Mac: `brew install kubernetes-cli`
  - Windows: `choco install kubernetes-cli`
  - Linux:
    ```bash
    sudo apt update
    sudo apt install kubectl
    ```
- **Skaffold** — Инструмент для автоматизации разработки.
  - Mac: `brew install skaffold`
  - Windows: `choco install skaffold`
  - Linux:
    ```bash
    curl -Lo skaffold https://storage.googleapis.com/skaffold/releases/latest/skaffold-linux-amd64
    chmod +x skaffold
    sudo mv skaffold /usr/local/bin
    ```
  
---

## 🚀 2. Быстрый старт

### 🏗️ Шаг 1: Создание постоянной инфраструктуры

**Выполните один раз** для создания storage, secrets и backup системы:

```bash
# Создаем постоянные ресурсы (не удаляются при перезапуске)
kubectl apply -f k8s/db-storage.yml
kubectl apply -f k8s/db-secrets.yml
kubectl apply -f k8s/backup.yml

# Проверяем что все создано
kubectl get pvc,secrets,cronjob
```

> **⚠️ Важно:** Эти ресурсы создаются один раз и не управляются Skaffold. Они сохраняют данные между перезапусками.

---

### 🛠️ Шаг 2: Выбор режима запуска

#### **Development режим (рекомендуется для разработки):**
```bash
# Запускает только приложение, сохраняет backup данные
skaffold dev
```

**Преимущества:**
- Быстрый перезапуск приложения
- Backup данные не теряются
- Идеально для разработки и тестирования

#### **Production режим (полная инфраструктура):**
```bash
# Запускает всю инфраструктуру включая backup
skaffold dev -p production
```

**Преимущества:**
- Вся система в одном месте
- Подходит для демонстрации
- Автоматическое создание всех ресурсов

---

### 🌐 Шаг 3: Доступ к приложению

После запуска проект будет доступен по адресу:

👉 http://127.0.0.1:8000

---

### 🔄 Шаг 4: Управление backup (независимо от режима)

**Создать backup вручную:**
```bash
kubectl create job --from=cronjob/postgres-backup manual-backup-$(date +%Y%m%d-%H%M%S)
```

**Восстановить из последнего backup:**
```bash
kubectl create job --from=cronjob/postgres-restore restore-latest
```

---

### 📋 Шаг 5: Полезные команды

**Проверить статус системы:**
```bash
kubectl get all
kubectl get pvc,secrets,cronjob
```
---

## 📋 3. Полезные команды

### Работа с подами (контейнерами)

Посмотреть состояние всех компонентов:

```bash
kubectl get all
```

Посмотреть логи конкретного пода:

```bash
kubectl logs -f <название_пода>
```

Зайти внутрь контейнера Django (например, для миграций):

```bash
kubectl exec -it deployment/django-app -- python manage.py migrate
```

---

### Масштабирование

Хотите проверить мощь K8s? Увеличьте количество копий Django прямо на лету:

```bash
kubectl scale deployment django-app --replicas=3
```

---

## 🛑 4. Остановка

Чтобы удалить все ресурсы из кластера, просто нажмите `Ctrl+C` в окне, где запущен `skaffold dev`.

Если нужно принудительно очистить всё:

```bash
skaffold delete
```

---

## 🧩 Запуск без Skaffold (ручной режим)

### Сборка образа

```bash
docker build -t music-platform .
```

### Загрузка образа в Minikube

**Важно:** Чтобы кластер увидел ваш локальный образ (не скачивая его из Docker Hub), его нужно загрузить во внутреннее хранилище Minikube:

```bash
minikube image load music-platform
```

Примените конфигурации и манифесты:

```bash
kubectl apply -f k8s/
```

Проверьте, что все поды поднялись:

```bash
kubectl get pods
```

---

## 🌐 Способы доступа к приложению

### Вариант А: Через Minikube 

Используйте встроенную функцию Minikube, которая сама откроет браузер на нужном IP-адресе:

```bash
minikube service django-service
```

---

### Вариант Б: Через Port Forwarding 

Если вам нужно, чтобы проект был доступен строго по адресу http://localhost:8000:

```bash
kubectl port-forward service/django-service 8000:8000
```

---

### [24.02.2026] — Оркестрация, отказоустойчивость и безопасность (Kubernetes)

**Добавлено:**
* **Init-контейнеры (Database Readiness):** Внедрен `initContainer` в манифест Django. Теперь приложение ожидает готовности порта PostgreSQL (5432) перед запуском основного процесса. Это решило проблему падения контейнера при холодном старте БД.
* **Liveness & Readiness Probes:**
    * В Django создан системный эндпоинт `/health/`.
    * Настроена **Readiness Probe**: Kubernetes не направляет трафик на под, пока Django полностью не загрузится.
    * Настроена **Liveness Probe**: Автоматический перезапуск контейнера в случае "зависания" Python-процесса.
* **Масштабирование и высокая доступность (High Availability):**
    * Количество реплик приложения увеличено до **2**. Теперь трафик балансируется между подами, что обеспечивает работу без простоев (Zero Downtime) даже при выходе одного из контейнеров из строя.
* **Централизованное управление конфигурацией (ConfigMap):**
    * Создан манифест `configmap.yaml` для хранения непубличных настроек (DEBUG, ALLOWED_HOSTS, настройки Redis). Это позволило отделить логику приложения от его окружения и менять параметры без пересборки Docker-образов.
* **Усиление безопасности (Secrets):**
    * Манифест `db-secrets.yml` был полностью переработан. Чувствительные данные (пароли, SECRET_KEY, API-токены) теперь хранятся в защищенном виде и динамически внедряются в среду выполнения.
* **Автоматизация жизненного цикла БД (Kubernetes Jobs):**
    * Миграции базы данных и сбор статики вынесены в отдельный объект `Job`. Это гарантирует, что структура БД обновится строго один раз перед запуском всех реплик приложения, исключая конфликты при одновременном старте.

### [25.02.2026] — Управление состоянием (Persistence), автоматизация зависимостей и оптимизация инфраструктуры

* **Отказоустойчивое хранение данных (PostgreSQL & Redis Persistence):**
    * **PostgreSQL:** Переведен на использование `PersistentVolumeClaim (PVC)`. Данные теперь сохраняются при перезагрузке или пересоздании подов. Реализованы пробы (`readiness/liveness`) через `pg_isready`.
    * **Redis:** Внедрена поддержка `PVC` для сохранения кэша и очередей задач. Активирован режим **AOF (Append Only File)** для гарантии сохранности данных на диске. Добавлены проверки здоровья через `redis-cli ping`.
    * **Единое хранилище:** Создан манифест `db-storage.yml`, централизованно описывающий дисковое пространство для всех сервисов БД.
* **Автоматизация и порядок запуска (InitContainers & Jobs):**
    * В Django-app добавлен **InitContainer** (`check-db-ready`), который блокирует старт приложения до полной готовности базы данных, исключая ошибки подключения.
    * Миграции БД и сборка статики полностью делегированы оркестратору через отдельные механизмы запуска, обеспечивая соответствие принципам **Stateless-контейнеров**.
* **Оптимизация ресурсов и отказоустойчивость (K8s Best Practices):**
    * **Resources & Limits:** Для всех контейнеров (Django, Postgres, Redis) внедрены `cpu` и `memory` requests/limits, что предотвращает деградацию нод кластера.
    * **RollingUpdate:** Настроены параметры `maxUnavailable` и `maxSurge` для обновления приложения без простоя трафика.
    * **PodDisruptionBudget (PDB):** Гарантирует наличие минимум одной активной реплики Django при плановом обслуживании кластера.
    * **Graceful Shutdown:** Настроен `terminationGracePeriodSeconds` (30с) для корректного закрытия активных соединений.
* **Безопасность и оптимизация Docker:**
    * **Принцип минимальных привилегий:** Реализован запуск приложения от имени не-привилегированного пользователя (`appuser`).
    * **Build Caching:** Оптимизирован порядок слоев в Dockerfile, что ускорило сборку образа в несколько раз.
    * **Image Slimming:** Реализована очистка кэша пакетного менеджера и использование alpine-образов для уменьшения размера и площади атаки.
**Глубокий мониторинг и самовосстановление (Deep Health Checks):**
    * **Smart Readiness Probe:** В Django реализован кастомный эндпоинт `/health/`, который выполняет комплексную диагностику: проверяет не только доступность HTTP-сервера, но и **активное соединение с PostgreSQL и Redis**. Это исключает попадание трафика на «битые» поды.
    * **Native Probes для БД:** Интегрированы нативные проверки здоровья: `pg_isready` для PostgreSQL и `redis-cli ping` для Redis, что позволяет Kubernetes корректно управлять жизненным циклом каждого компонента.

### [25.02.2026] — Стратегия резервного копирования

* **Автоматизированное резервное копирование PostgreSQL:**
    * **CronJob для бэкапов:** Внедрен `CronJob` (`postgres-backup`) для ежедневного автоматического создания бэкапов базы данных в 2:00 ночи.
    * **Вращение бэкапов:** Настроена автоматическая очистка старых бэкапов (хранение 7 дней) для оптимизации дискового пространства.
    * **Надежное хранение:** Создан отдельный `PersistentVolumeClaim` (`backup-pvc`) на 2GB для надежного хранения бэкапов.
    * **Сжатие данных:** Бэкапы автоматически сжимаются через `gzip` для экономии места.
    * **Resource Management:** Настроены resource limits (CPU: 100m/500m, Memory: 128Mi/512Mi) для защиты от OOM и предотвращения "съедания" ресурсов ноды.
* **Инструменты восстановления данных:**
    * **Restore Job:** Подготовлен `Job` (`postgres-restore`) для автоматического восстановления из последнего бэкапа.
    * **Умное восстановление:** Job автоматически находит самый свежий backup файл (`ls -t`) и восстанавливает базу данных.
    * **Валидация и логирование:** Проверка наличия backup файла, детальные логи процесса восстановления и обработка ошибок.
    * **Resource Limits:** Аналогичные ограничения ресурсов для стабильности процесса восстановления.
    * **Гибкость восстановления:** Поддержка восстановления из любого доступного бэкапа в хранилище.
* **Production-Ready инфраструктура:**
    * **Полная готовность к продакшену:** Проект теперь соответствует enterprise-стандартам по надежности и безопасности данных.
    * **Disaster Recovery:** Реализована полная стратегия восстановления после сбоев.
    * **Compliance готовность:** Архитектура соответствует требованиям по сохранности данных для коммерческих проектов.